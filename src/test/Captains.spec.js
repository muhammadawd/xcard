import {mount} from '@vue/test-utils'
import AllCaptains from '../views/modules/captains/all/Index.vue';

describe('AllCaptains', () => {
  test('setup correctly', () => {
    expect(true).toBe(true);
    // expect(true).toBe(false)
  });
});
